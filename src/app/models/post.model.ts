export interface Post {
  id: string;
  username: string;
  audioURL: string;
  audioDuration: number;
  likes: string[];
  likesCount: number;
  dislikesCount: number,
  playsCount: number,
  createdAt: string;
}
